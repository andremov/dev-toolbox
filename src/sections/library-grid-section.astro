---
import LibraryTile from '../components/library-tile.astro';
import data from '../utils/libraries';
---

<div class="container mx-auto grid grid-cols-1 md:grid-cols-2 gap-2 mb-8 place-items-center">
  {
    data.map((library) => (
      <LibraryTile {...library} />
    ))
  }
</div>
